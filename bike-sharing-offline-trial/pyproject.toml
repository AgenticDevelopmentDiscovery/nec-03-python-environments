[project]
name = "bike-sharing-offline-trial"
version = "0.1.0"
description = "Bike-sharing regression exercise, packaged for a reproducible venv/lockfile demo"
requires-python = ">=3.10,<3.11"
dependencies = [
    "numpy==1.22.4",
    "scipy>=1.7.3",
    "matplotlib==3.5.3",
]

[tool.uv]
package = false
